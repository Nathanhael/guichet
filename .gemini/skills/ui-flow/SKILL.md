\---

name ui-flow

description Debugs React components and Zustand store synchronization.

\---

\# Instructions

\- Inspect `clientsrcstoreuseStore.ts` for functional update errors.

\- Verify that `SupportView.tsx` and `AgentView.tsx` are listening for the correct `messagenew` events.

\- MANDATE Use `docker compose exec client` for frontend testing.

